@page
@model IndexModel
@{
    ViewData["Title"] = "Entrevista";
}

<div class="text-center">
    <h1 class="display-4">Bem vindo 🙋‍♂️</h1>
    <p class="lead">Caro amigo, este projeto foi desenvolvido com o intuito de testar suas habilidades com programação. Não se preocupe com os detalhes, queremos apenas verificar o seu nível de entendimento das prevalentes funções do dia-a-dia. Boa sorte!!</p>
</div>

<!-- Matheus André -->


<div class="row">
    <div class="col-md-12">
        <h1>LISTA DE TAREFAS</h1>
    </div>
</div>

<form asp-page-handler="post" name="formTarefa">
    @Html.AntiForgeryToken()

    @for(var i = 0; i < Model.Tarefas.Count(); i++)
    {
        <div class="row">
        <div class="col-md-12">
             @Html.HiddenFor(m => Model.Tarefas[i].Id)
             @Html.HiddenFor(m => Model.Tarefas[i].Tipo)
             @Html.HiddenFor(m => Model.Tarefas[i].Descricao)

             @{
                 var htmlAttributes = new System.Collections.Generic.Dictionary<string, object>();
                 if (User.IsInRole("Admin")) {
                     htmlAttributes.Add("onclick", "document.formTarefa.submit()");
                 } else {
                     htmlAttributes.Add("disabled", "disabled");
                 }
             }
             @Html.CheckBoxFor(m => Model.Tarefas[i].Concluida, htmlAttributes)
             <b>[@Html.DisplayFor(m => Model.Tarefas[i].Tipo)]</b>
             @Html.DisplayFor(m => Model.Tarefas[i].Descricao)
        </div>
    </div>
    }

</form>

<br/>
<br/>

<div class="row">
    <div class="col-md-12">
        <h1>DICAS</h1>
    </div>
</div>

<ul>
    <li>Se alguma tarefa estiver muito difícil, entregue o que conseguir, mesmo que não seja exatamente o que foi solicitado (não gaste mais do que 10 minutos em uma mesma tarefa);</li>
    <li>Para zerar o banco de dados, basta excluir o arquivo banco.db (será gerado na raiz do projeto);</li>
    <li>Priorize as tarefas de <b>[♻️refactor]</b>, elas são as mais importantes;</li>
    <li>Se seu visual studio der suporte, selecione o target framework do projeto em .NET6 (ele permitirá usar o hot reload o que te permitirá ganhar tempo);</li>
    <li>Apesar de não ser necessário, você pode instalar bibliotecas em que se sinta mais confortável para trabalhar (como por exemplo alguma de injeção de dependências);</li>
    <li>Ao final do projeto, se estiver com alguma dificuldade para fazer o push do seu branch para o repositório, não se preocupe, entregue o projeto compactado em um arquivo .zip mesmo;</li>
</ul>
